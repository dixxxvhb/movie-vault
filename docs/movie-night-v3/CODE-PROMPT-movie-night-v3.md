Run the Movie Night v3 overhaul. Repo is this one (`~/Code/movie-vault`). Supabase project `swjqlfcqvcrnydpyjyog` via the Supabase MCP. Two input files are in `docs/movie-night-v3/`: `MOVIE-NIGHT-V3-BRIEF.md` (diagnosis and design, read it fully first) and the drafted skill folder `movie-night-v3/` (SKILL.md plus `references/`). Dixon's rulings, already made: whole overhaul in one session; memory scores move into the registry; seven emotional keys drafted by Leonard for Dixon to edit; `film_mailbox` is repurposed as the Code/Leonard channel; the skill is split into SKILL.md plus references; Inside Man and Se7en hot takes stay empty (Dixon's call), they will show as debt and that is correct.

Rules for this session: no em dashes in anything Dixon-facing (SQL comments and code are fine). Before any UPDATE or DELETE on his data, run the WHERE as a bare SELECT first and print the row count. Never touch a rating. Never delete a lesson; supersede with active = false. Keep the taste profile a single row. Ask before anything destructive that the brief does not name.

Do it in this order, one migration file per numbered step under `supabase/migrations/` (or the repo's existing convention; check first), applied with the MCP apply_migration:

1. Extensions and columns. `create extension if not exists pg_trgm`. Add to `film_titles`: `memory_score numeric(3,1)`, `seen_bucket text check (seen_bucket in ('R','F','L','C','?'))`, `abandoned_on date`, `abandon_note text`, `certified_on date`, `certified_score numeric(3,1)`, `certified_line text`. Add to `film_log`: `emotional_key text`. Create `film_vetoes (id uuid pk, title_id uuid references film_titles unique, reason text, created_at)` with the same RLS as the other film tables. Trigram index on `film_titles.title` and `film_recommendations.suggested_title`.

2. Backfill from the taste profile and the registry. Read `film_taste_profile.content -> 'vault_archive'` (latest row). For every entry in `scored` set `memory_score` and `seen_bucket` on the matching title (match by title ilike; franchise entries map to the registry rows that already exist, e.g. 'Mission: Impossible', 'The Bourne trilogy', 'The Lord of the Rings (trilogy)', 'Planet of the Apes (the modern trilogy)', 'James Bond (the franchise)'; insert thin rows for any that do not exist). Every `hazy_seen_unrated` entry gets `seen_before = true`, no score, bucket from the seen_note if it says long ago / few years / childhood, else '?'. Also parse existing `seen_note` text of the form 'memory N.N' into `memory_score` where the column is still null. `archive_additions` (Whiplash 8.7, Parasite 9.6) and `certified_films` (Amadeus 9.3 on 2026-07-27 with its line, Predestination 9.1 on 2026-08-10) go to the title rows. Cosmos gets `abandoned_on = '2026-08-19'` and its note moved to `abandon_note`. Prisoners goes into `film_vetoes` with reason 'only Dixon raises it'. Print a before/after table.

3. Repair the drift. Set `seen_before = true` on every title with a `film_log` row (expect 32 changes) and fill `seen_note` with 'Ledger film, watched <date>, rated <n>' where empty. Stardust: remove from hazy (it is on the wall). For every `film_recommendations` row with null `title_id`, resolve `suggested_title` to a title by trigram (strip a trailing '(year)' or ' — rewatch' first; match year within 1 if present); print the proposed mapping and apply; insert thin title rows for the unmatched. Then close every open `suggested` rec whose title has a `film_log` row (set `accepted`). Then `alter table film_recommendations alter column title_id set not null`.

4. Triggers. Replace `film_log_resolves_recommendations` with a function that, on `film_log` insert: flips watchlist (keep existing), sets `seen_before = true` and a default `seen_note` when empty, closes open recs by `title_id` and by trigram on `suggested_title` against the title's name, and, when `new.is_rewatch` and the title has a `memory_score`, appends `{title, memory_score, live_score, watched_at}` to the latest `film_taste_profile.content -> 'vault_model' -> 'paired_measurements'` (update that single row in place). Add a before-insert trigger on `film_recommendations` that resolves a null `title_id` from `suggested_title` (trigram, year-aware) or inserts a thin title row. Add a check so `certified_on`, `certified_score` and `certified_line` are set together.

5. Views and functions, all `security invoker`, granted to authenticated, matching `references/schema.md` exactly in names and columns:
   - `film_status` view. Precedence: ledger > certified > archive (memory_score not null) > hazy (seen_before, no memory_score, not abandoned) > seen (seen_before with a note that says confirmed seen, no bucket) > abandoned > veto > queued > watching > pitched (open suggested rec) > dismissed > fresh. Columns: `title_id, title, year, media_type, state, live_score, live_watched_at, is_rewatch_seen (count of log rows > 1), memory_score, seen_bucket, certified_score, open_rec_id, last_pitched_on, pitch_count, runtime_minutes, providers (flatrate/free/ads names from watch_providers), provenance (coalesce of watchlist added_reason, seen_note, abandon_note)`.
   - `film_check(q text)` returning `film_status` columns plus `similarity` and `verdict` text, `similarity(title, q) > 0.35`, ordered by similarity desc, year desc. Verdict strings: `ON THE WALL <score> (<date>)`, `CERTIFIED <score> (<date>)`, `ARCHIVE memory <score>, rewatch only, disclose`, `HAZY, rewatch only, disclose`, `SEEN, unscored, confirm before pitching`, `ABANDONED <date>: <note>`, `VETO: only Dixon raises it`, `QUEUED (<provenance>)`, `WATCHING`, `OPEN REC since <date>`, `DISMISSED <date>`, `FRESH`.
   - `film_night_debt` view: one row per (film_log row, missing item) for hot_take, vibe_tags, emotional_key, panel (no `film_ledger_panels` row for the title), note (no `film_session_notes` row with `note_date = watched_at`), plus a synthetic `wall_behind` row listing titles logged after the newest `film_mailbox` note that starts with 'published through'.
   - `film_session_brief()` returning jsonb: `now` (America/New_York), `last_nights` (last 7 distinct dates: title, score, emotional_key, vibe_tags, watched_at), `queue` (queued + watching with provenance and runtime), `open_recs` (title, age_days, source), `services`, `lessons_digest` (`law`: weight 5 rules as one-liners; `taste`: weight >= 3 scope taste in full; `ritual`: weight >= 3 scope ritual/protocol in full), `saturation` (any film_log rating >= 9.5 in the last 7 days with its top vibe_tag and `closes_on = watched_at + 7`), `last_note` (most recent session note, full), `debt` (from the view), `mailbox` (unread), `emotional_keys` (from the profile).
   - `film_audit_menu(lane text, n int)`: n `fresh` titles from `film_status` whose genres or a known vibe lane match `lane` (ilike on genres array or title tags), random order, excluding anything pitched in the last 30 days.
   - `film_rewatch_shelf` view: hazy first, then archive by memory_score desc, with provenance.
   - `film_retro()` jsonb: log vs panel parity, open recs older than 21 days, titles with null tmdb_id or null watch_providers, weight-5 lessons with empty evidence, profile keys matching `vault_counts%` or `current_mood`, mailbox unread count.

6. Profile scrub (single row, update in place). Remove keys `current_mood`, `vault_counts_20260725`, `vault_counts_20260726`, `vault_archive`, and `vault_model.archive_additions`, `vault_model.certified_films` (now on the registry). Add `emotional_keys: ["tense","dread","fun","cozy","awe","sad","camp"]`. Rewrite `lanes` entries and `notes_md` with no em dashes and with any remaining 5-point scores converted to the 10-point era or removed (rating_anchors already documents the rescale). Keep everything else. Print the final key list.

7. Lessons hygiene. Merge the three seen-check rows (2026-08-03, 2026-08-14, 2026-08-25) into one weight-5 row worded around `film_check` ('no title is pitched without its film_check verdict on the card'), superseding the three. Set `active = false` on the two dead-pipeline rows (2026-07-26 restore.py, 2026-07-28 vault.py drift guard) with evidence 'superseded by film_status precedence and the v3 pipeline doc'. Set `active = false` on the 2026-07-21 'update the Vault artifact' row (the artifact is retired). Insert one new weight-5 protocol row dated 2026-08-26: 'Rules for the model, constraints for the database: a weight-5 protocol rule broken twice becomes a trigger, view or function, not a louder rule. Evidence: 32 stale seen flags, 42 orphan recs, three seen-check escalations.' Backfill `emotional_key` on all 44 film_log rows from the hot takes, tags and session notes; print the table for Dixon to eyeball.

8. Edge function `film-recs`: replace the seen-guard inputs with `film_status` (states other than fresh, queued, watching, pitched are excluded; archive and hazy allowed only when the request asks for rewatches), feed `lessons_digest` from the brief function instead of the full profile blob, and keep the rest. Deploy. Also deploy a tiny `film-hydrate` invocation (or reuse `film-tmdb`) across titles missing `tmdb_id` or `watch_providers`, and schedule a nightly refresh of `watch_providers` for queued and open-rec titles (pg_cron if enabled, else the dispatch function).

9. Pipeline. Update `scripts/emit_vault_data.py` and the data pull so `archive.json` is built from `memory_score` and `seen_before` columns, not from parsing `seen_note`. Fix the README paragraph that describes the Investigation wall as red string strung across the ledger so it matches the Thread ruling (opt-in, on hold, gone on release). Re-pull data, `npm run data`, commit, push, verify live, then insert the first mailbox marker: 'published through <newest slug> <date>'.

10. Skill. Move `docs/movie-night-SKILL.md` to `docs/movie-night/SKILL.md` and put the drafted `references/` beside it, replacing the drafts' function descriptions with whatever you actually shipped if names or columns differ (they should not). Add the Leonard drafts' seven keys to SKILL.md nowhere; they live in the profile. Run `package_skill.py` from skill-creator on `docs/movie-night/`, save the `.skill` to `docs/movie-night/movie-night.skill`, and tell Dixon exactly where it is so he can re-save it.

11. Smoke test through the MCP and paste the results: `select film_session_brief()`; `select verdict from film_check('the matrix')` (expect ON THE WALL); `select verdict from film_check('masters of the universe')` (expect two rows, 2026 on the wall, 1987 whatever it is); `select verdict from film_check('prisoners')` (expect VETO); `select verdict from film_check('the fugitive')` (expect OPEN REC); `select * from film_night_debt` (expect Inside Man and Se7en rows for hot_take, vibe_tags, emotional_key, panel, and nothing for wall_behind after step 9); `select count(*) from film_recommendations where title_id is null` (expect 0); `select count(*) from film_titles t where exists (select 1 from film_log l where l.title_id = t.id) and not seen_before` (expect 0).

End with a short report: what shipped, anything you deviated on and why, the `.skill` path, and the debt list as it stands.
